export const checkIfEven = (n: number): boolean => {
    if (n % 2 == 0) {
        return true
    }

    return false
}

// export const checkIfEvenOneLiner = (n: number): boolean => {
//     return n % 2 == 0
// }

// export const checkIfEvenAbbrev = (n: number): boolean => (n % 2 == 0)